---
title: 'Eigenvectors and Eigenvalues'
titleZh: '特征向量与特征值'
description: 'The special arrows that hold their direction through a transformation — they only stretch, never rotate.'
descriptionZh: '在变换中保持方向不变的特殊箭头——它们只被拉伸，从不旋转。'
course: 'linear-algebra'
module: 'Orthogonality and Decomposition'
moduleZh: '正交与分解'
moduleOrder: 5
lessonOrder: 25
duration: 22
difficulty: 'Advanced'
---

import { KeyInsight } from '../../../components/learning/KeyInsight';
import { ExerciseBlock } from '../../../components/learning/ExerciseBlock';
import { MathBlock, MathInline } from '../../../components/math/Math';
import { EigenvectorVis } from '../../../components/interactive/linear-algebra/EigenvectorVis';
import { EigenDecomposition3DCompanion } from '../../../components/interactive/linear-algebra/EigenDecomposition3DCompanion';
import { FormulaReadout } from '../../../components/interactive/FormulaReadout';

# Eigenvectors and Eigenvalues

When a matrix warps space, most vectors get knocked off their original direction — they point somewhere new. But a few special vectors **keep their direction**. They might get stretched or shrunk, but they don't rotate. These are the **eigenvectors**, and their stretch factors are the **eigenvalues**.

This is one of the deepest ideas in linear algebra. Eigenvectors reveal the "skeleton" of a transformation — the axes along which it acts most simply.

## The equation

An eigenvector **v** of a matrix A satisfies:

<MathBlock expression={'A\\vec{v} = \\lambda \\vec{v}'} />

Lambda (λ) is the eigenvalue. Read this geometrically: applying A to **v** gives the same direction **v**, just scaled by λ. No rotation. No shear. Pure stretch.

## Finding eigenvalues

Rearrange the equation:

<MathBlock expression={'(A - \\lambda I)\\vec{v} = \\vec{0}'} />

This has a nonzero solution **v** only if det(A - λI) = 0. So eigenvalues are the roots of the **characteristic polynomial** det(A - λI).

For a 2x2 matrix with entries a, b, c, d:

<MathBlock expression={'\\lambda^2 - (a+d)\\lambda + (ad - bc) = 0'} />

### Trace and determinant reveal everything

Two numbers already sitting in the matrix encode everything about its stretching behavior:

- **trace = λ₁ + λ₂**: The trace (a + d) is the *sum* of the eigenvalues. It measures the "average stretching" — a trace of 3 means the transformation expands by 1.5 on average.
- **det = λ₁ · λ₂**: The determinant (ad - bc) is the *product* of the eigenvalues. It measures the total area scaling — a determinant of 6 means the transformation multiplies areas by 6.

These two numbers, already visible at a glance, determine the eigenvalues completely. You can find them without solving the characteristic polynomial if you know the trace and determinant: the eigenvalues are the two numbers that add to the trace and multiply to the determinant.

## Find the eigenvectors

The circle of blue vectors below shows what happens to *every* direction under a transformation. The yellow and red arrows are the eigenvectors — they stay on their line, only stretching. Click a preset and watch the animation, or drag the "Transform" slider manually.

<EigenvectorVis client:visible />

## Visual examples

Click each preset and observe:

- **"Shear"**: Only the x-axis stays on its line (eigenvalue 1). All other vectors get tilted. The transformation has only one eigendirection — horizontal.

- **"Rotation"**: The banner reads "No real eigenvalues." Rotation moves every vector off its line — the eigenvalues are imaginary numbers (±i), reflecting the rotation's spiraling nature. No direction is respected.

- **"Symmetric"**: Both eigenvectors are **perpendicular to each other**. This is not a coincidence — symmetric matrices (where A = Aᵀ) always have orthogonal eigenvectors. The symmetric preset (a=2, b=1, c=1, d=2) has eigenvectors along (1,1) and (1,-1), which are at right angles.

- **"Diagonal"**: The eigenvectors are exactly the coordinate axes. For a diagonal matrix, the eigenvalues are sitting right on the diagonal — nothing to compute.

- **"Defective"**: This matrix [[2,1],[0,2]] has eigenvalue 2 (repeated), but only **one** eigendirection — the x-axis. The algebra says there should be two eigenvectors, but geometry says there's only one. This is a **defective** matrix.

### Defective matrices

Most matrices have a full set of eigenvectors. But some don't. The matrix [[2,1],[0,2]] has eigenvalue 2 with **algebraic multiplicity** 2 (it appears twice as a root of the characteristic polynomial), but **geometric multiplicity** only 1 (only one linearly independent eigenvector exists).

Geometrically, this means the transformation has only one "preferred axis." Along the x-axis, vectors are stretched by 2. Everywhere else, vectors are both stretched and shoved sideways. The eigenvector isn't enough to build a complete coordinate system for this transformation — you need a generalized eigenvector to fill the gap.

## Spectral decomposition

For a symmetric matrix (A = Aᵀ), the eigenvectors form an orthonormal basis, and the matrix can be written as a sum of **projection operators** onto its eigen-directions:

<MathBlock expression={'A = \\lambda_1 \\vec{v}_1 \\vec{v}_1^T + \\lambda_2 \\vec{v}_2 \\vec{v}_2^T'} />

Each term is a rank-1 matrix: vᵢvᵢᵀ projects onto the eigenvector's direction, and λᵢ scales the projection. The matrix is completely described by what it does along each eigen-direction — a sum of independent stretches.

This decomposition is the 2D version of what SVD does for any matrix (lesson 27). For symmetric matrices, eigendecomposition and SVD coincide.

## Complex eigenvalues

Not all eigenvalues are real. The rotation matrix [[0,-1],[1,0]] (90° rotation) has characteristic polynomial λ² + 1 = 0, giving λ = ±i. The eigenvalues are purely imaginary.

Geometrically, this makes sense: rotation moves every vector off its line, so no real direction is preserved. The imaginary eigenvalues encode the rotation's "spiraling" nature. For a matrix with complex eigenvalues λ = a ± bi, the transformation involves both rotation (the imaginary part b) and scaling (the real part a). The factor e^a controls growth/decay, and the angle arctan(b/a) controls rotation.

## The Gram matrix

The matrix G = AᵀA (called the **Gram matrix**) always has real, non-negative eigenvalues. Its eigenvalues are the squares of the singular values of A: if A has singular values σ₁, σ₂, then G has eigenvalues σ₁², σ₂².

This is the algebraic bridge between eigendecomposition (lesson 25) and SVD (lesson 27): the singular values of A are the square roots of the eigenvalues of AᵀA. The right singular vectors of A are the eigenvectors of AᵀA.

## In three dimensions

A 3D diagonal matrix stretches the unit circle into a 3D ellipsoid. The colored arrows are eigenvectors — each stays on its axis, stretched by its eigenvalue.

<EigenDecomposition3DCompanion client:visible mode="eigen" />

<FormulaReadout formulas={[
  { label: "特征方程", expression: "A\\vec{v} = \\lambda\\vec{v}" },
  { label: "3D 特征多项式", expression: "\\det(A - \\lambda I) = 0" },
]} />

<KeyInsight>
Eigenvectors are the directions a transformation respects — they only stretch, never rotate. The eigenvalues tell you how much they stretch. The trace (sum of λ) and determinant (product of λ) encode everything in two numbers already visible in the matrix.
</KeyInsight>

## Why eigenvectors matter

Eigenvectors give you the **natural coordinate system** for a transformation. If you rewrite a matrix in its eigenbasis, it becomes diagonal — just a list of stretch factors. Complex transformations simplify to independent stretches along independent axes.

This idea powers:
- **Differential equations**: Solutions are combinations of eigenvectors, each evolving independently at its own rate e^(λt). The eigenvalues determine whether solutions grow, decay, or oscillate.
- **Principal component analysis**: The eigenvectors of the covariance matrix point along the most "interesting" directions in your data — where the variance is largest.
- **Google's PageRank**: The steady-state distribution of web surfing is an eigenvector of the link matrix with eigenvalue 1 — the distribution that doesn't change when you click another link.
- **Quantum mechanics**: Observable values (energy, momentum, spin) are eigenvalues of Hermitian operators, and the measured states are eigenvectors — the only states that give a definite answer when you measure.

## Connections

Eigenvectors are the natural coordinate system of a transformation. In lesson 26, diagonalization rewrites a matrix in its eigenbasis — making it as simple as a list of stretch factors. In lesson 27, SVD extends this idea to non-square matrices by using two different bases (input and output).

The spectral decomposition A = Σλᵢvᵢvᵢᵀ is the symmetric-matrix version of the SVD's A = Σσᵢuᵢvᵢᵀ. The Gram matrix G = AᵀA is the algebraic bridge: its eigenvectors are the right singular vectors, and its eigenvalues are the squared singular values.

## Practice

<ExerciseBlock
  number={1}
  prompt="Find the eigenvalues and eigenvectors of [[3,0],[0,5]]. What does this transformation do?"
  hint="This is already diagonal — the eigenvalues are sitting on the diagonal."
  answer="Eigenvalues λ₁=3, λ₂=5. Eigenvectors: (1,0) for λ₁, (0,1) for λ₂. The transformation stretches x by 3 and y by 5. The basis vectors are eigenvectors because the matrix is diagonal."
/>

<ExerciseBlock
  number={2}
  prompt="Find the eigenvalues of [[2,1],[1,2]]. For each eigenvalue, find an eigenvector."
  hint="Characteristic polynomial: λ² - 4λ + 3 = 0. Roots: λ = 1 and λ = 3."
  answer="λ₁=1: (A-I)v=0 → [[1,1],[1,1]]v=0 → v₁=(1,-1). Direction along y=-x, eigenvalue 1 (unchanged). λ₂=3: (A-3I)v=0 → [[-1,1],[1,-1]]v=0 → v₂=(1,1). Direction along y=x, eigenvalue 3 (stretched by 3)."
/>

<ExerciseBlock
  number={3}
  prompt="A 2×2 matrix has trace = 5 and determinant = 6. Find its eigenvalues without writing the characteristic polynomial."
  hint="Find two numbers that add to 5 and multiply to 6."
  answer="λ₁ + λ₂ = 5 and λ₁ · λ₂ = 6. The two numbers that satisfy this are 2 and 3. So λ₁=2, λ₂=3. The trace-determinant shortcut avoids solving the quadratic entirely."
/>

<ExerciseBlock
  number={4}
  prompt="Write the spectral decomposition of A = [[3,1],[1,3]]. Verify that it equals A."
  hint="Eigenvalues are 4 and 2. Eigenvectors: v1 = (1,1)/√2, v2 = (1,-1)/√2."
  answer="A = 4·v₁v₁ᵀ + 2·v₂v₂ᵀ = 4·[[0.5,0.5],[0.5,0.5]] + 2·[[0.5,-0.5],[-0.5,0.5]] = [[2,2],[2,2]] + [[1,-1],[-1,1]] = [[3,1],[1,3]] ✓. The matrix is a weighted sum of projections onto its eigen-directions."
/>

<ExerciseBlock
  number={5}
  prompt="The rotation matrix [[cos θ, -sin θ],[sin θ, cos θ]] has eigenvalues cos θ ± i sin θ. For θ = 90°, what are the eigenvalues? What does this tell you geometrically?"
  hint="cos 90° = 0, sin 90° = 1. So eigenvalues are ±i."
  answer="Eigenvalues are λ = ±i. They're purely imaginary — no real stretch factor exists. This confirms geometrically that 90° rotation preserves no direction in the real plane. Every vector gets rotated away from its original line."
/>

---

**Up next:** [Diagonalization](/courses/linear-algebra/26-diagonalization)
